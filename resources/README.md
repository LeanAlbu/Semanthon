# Resources

Place your `logs.jsonl` file in this directory for the ingestion script to process it.

The file should be in JSON Lines format with at least the following fields:
- `timestamp`
- `source_ip`
- `message`
- `target_port`
- `http_method`
